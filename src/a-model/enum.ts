/**
 * @fileOverview input UI component enum
 * @author Max
 */

export enum EDIT_TYPE {
    INPUT_STRING,
    SELECT_SINGLE,
    INPUT_INT,
    INPUT_FLOAT,
    TEXTAREA,
    DATE,
    DATE_TIME,
    SELECT_MULTIPLE,
    CHECKBOX,
    IMAGE,
    URL
}

export enum FIELD_TYPE {
    STRING,
    INT,
    FLOAT,
    TIMESTAMP,
    BOOLEAN,
    IMAGE
}